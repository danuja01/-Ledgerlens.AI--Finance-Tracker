# Copy to .env — Vite only exposes variables prefixed with VITE_
# https://vitejs.dev/guide/env-and-mode.html
# On Vercel: Project → Settings → Environment Variables (same names for Production/Preview).
VITE_OPENAI_API_KEY=
